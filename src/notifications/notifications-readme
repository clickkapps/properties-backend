npm install nodemailer ejs
npm install --save-dev @types/nodemailer @types/ejs
npm install -D @maizzle/cli // for compiling tailwind into email template
npm install -D @maizzle/framework tailwindcss postcss autoprefixer

- to notify

import { notify } from './notifications/dispatcher';

notify({
  to: 'user@example.com',
  channel: 'email',
  subject: 'Welcome!',
  templateName: 'welcome',
  variables: { name: 'Daniel' }
});